import CommercialScanClient from './CommercialScanClient'

export const metadata = { title: 'Connexion Commercial — L&Lui' }

export default function CommercialPage() {
  return <CommercialScanClient />
}
